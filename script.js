function searchCrane() {
    const query = document.getElementById('searchBar').value;
    alert(`Searching for: ${query}`);
    // Add functionality to redirect or filter cranes based on search query
  }